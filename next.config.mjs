/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "incor-ranking.s3.us-east-1.amazonaws.com",
      "incorcentromedico.com.ar",
    ],
  },
};

export default nextConfig;
